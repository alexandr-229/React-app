import React from 'react';
import { Input } from '../input';
import { CardList } from '../CardList';

export const Home = () => {
    return(
        <div>
            <Input />
            <CardList />
        </div>
    )
}